name: "Hugo Docker Fresh"
run-name: >-
  ${{ format('Benchmark {0} | {1}', inputs.cli_version != '' && 'Canary' || 'Fresh', github.event_name == 'schedule' && 'Weekly' || format('Manual dispatch ({0})', github.ref_name)) }}

on:
  workflow_dispatch:
    inputs:
      prewarm_buildkit_image:
        description: "Optional BuildKit image. When set, the run adds a BC BuildKit backend lane."
        required: false
        type: string
        default: ""
      cli_version:
        description: "Optional BoringCache CLI release tag for canary runs, for example vcli-canary."
        required: false
        type: string
        default: ""
      cli_platform:
        description: "Optional BoringCache CLI platform. Canary currently publishes linux-amd64."
        required: false
        type: string
        default: linux-musl-amd64
      benchmark_id_suffix:
        description: "Optional suffix appended to built-in benchmark/cache scopes for isolated comparison runs."
        required: false
        type: string
        default: ""
  schedule:
    - cron: "0 3 * * 1"

concurrency:
  group: ${{ github.workflow }}-${{ github.ref_name }}
  cancel-in-progress: false

permissions:
  contents: read
  actions: write
  packages: read

jobs:
  github-actions-cache:
    name: GHA
    uses: ./.github/workflows/reusable-docker-buildkit-benchmark.yml
    with:
      strategy: actions-cache
      cache_lane: fresh
      benchmark_id: ${{ format('hugo{0}', inputs.benchmark_id_suffix) }}
      project_repo: gohugoio/hugo
      dockerfile_path: upstream/Dockerfile
      docker_context: upstream
      image_tag: hugo-benchmark:latest
      benchmark_workspace: ${{ github.repository }}
    secrets: inherit

  boringcache-oci:
    name: BC OCI
    uses: ./.github/workflows/reusable-docker-buildkit-benchmark.yml
    with:
      strategy: boringcache
      cache_lane: fresh
      buildkit_backend: registry
      benchmark_id: ${{ format('hugo{0}', inputs.benchmark_id_suffix) }}
      project_repo: gohugoio/hugo
      dockerfile_path: upstream/Dockerfile
      docker_context: upstream
      image_tag: hugo-benchmark:latest
      cli_platform: linux-amd64
      cli_version: ${{ inputs.cli_version }}
      benchmark_workspace: ${{ github.repository }}
    secrets: inherit

  boringcache-buildkit-backend:
    name: BC BuildKit Backend
    uses: ./.github/workflows/reusable-docker-buildkit-benchmark.yml
    with:
      strategy: boringcache
      cache_lane: fresh
      buildkit_backend: registry
      buildkit_cache_backend: boringcache
      benchmark_id: ${{ format('hugo-bc-buildkit{0}', inputs.benchmark_id_suffix) }}
      project_repo: gohugoio/hugo
      dockerfile_path: upstream/Dockerfile
      docker_context: upstream
      image_tag: hugo-benchmark:latest
      cli_platform: linux-amd64
      cli_version: ${{ inputs.cli_version }}
      buildkit_image: ${{ inputs.prewarm_buildkit_image || vars.HUGO_PREWARM_BUILDKIT_IMAGE || 'ghcr.io/boringcache/buildkit:v0.30.0-bc' }}
      benchmark_workspace: ${{ github.repository }}
    secrets: inherit
